import { StyleSheet, Text, View } from "react-native";
import { PageHeader, Screen } from "../src/ui";
import { colors, type } from "../src/theme";
const items = [{ title: "New hire request", body: "Bristol Movement Lab requested the Dance Studio.", time: "18 min ago", new: true }, { title: "Booking accepted", body: "Neighbourhood Shorts is confirmed for 7 August.", time: "Yesterday", new: false }, { title: "Profile reminder", body: "Add an emergency contact to complete the venue profile.", time: "23 Jul", new: false }];
export default function HostNotificationsScreen() { return <Screen><PageHeader title="Host notifications" eyebrow="The Island" />{items.map((item) => <View key={item.title} style={styles.item}>{item.new ? <View style={styles.dot} /> : <View style={styles.dotSpace} />}<View style={{ flex: 1 }}><Text style={type.h3}>{item.title}</Text><Text style={styles.body}>{item.body}</Text><Text style={styles.time}>{item.time}</Text></View></View>)}</Screen>; }
const styles = StyleSheet.create({ item: { marginHorizontal: 20, paddingVertical: 17, flexDirection: "row", gap: 10, borderBottomWidth: 1, borderColor: colors.ink }, dot: { width: 9, height: 9, borderRadius: 5, backgroundColor: colors.pink, marginTop: 6 }, dotSpace: { width: 9 }, body: { marginTop: 4, color: colors.muted, fontSize: 13, lineHeight: 18 }, time: { marginTop: 7, color: colors.pink, fontSize: 10, fontWeight: "800" } });
