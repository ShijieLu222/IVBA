import { StyleSheet, Text, View } from "react-native";
import { AppIcon, InfoBanner, PageHeader, PrimaryButton, Screen } from "../src/ui";
import { colors, type } from "../src/theme";
const steps = [["search", "Find a characterful space", "Compare capacity, access, price guidance and availability."], ["document-text-outline", "Send one clear request", "Share dates, audience size and what you need to make."], ["people-outline", "Agree details together", "Receive a quote, ask questions and keep decisions in one place."]] as const;
export default function ForOrganisersScreen() { return <Screen><PageHeader eyebrow="FOR ORGANISERS" title="Make something happen in Bristol" /><View style={styles.hero}><Text style={styles.lede}>Find an independent space, agree the details and bring your event to life.</Text></View><View style={styles.steps}>{steps.map(([icon, title, body], index) => <View key={title} style={styles.step}><View style={styles.number}><Text style={styles.numberText}>{index + 1}</Text></View><AppIcon name={icon} size={26} color={colors.pink} /><View style={{ flex: 1 }}><Text style={type.h3}>{title}</Text><Text style={styles.body}>{body}</Text></View></View>)}</View><InfoBanner icon="open-outline" title="Organiser tools live on the web" body="Venue requests and event management open in a secure web workspace." tone="yellow" /><View style={styles.action}><PrimaryButton label="Explore spaces" tone="black" /><PrimaryButton label="Open organiser workspace" /></View></Screen>; }
const styles = StyleSheet.create({ hero: { padding: 20, paddingTop: 0 }, lede: { fontSize: 21, lineHeight: 29, fontWeight: "600" }, steps: { margin: 20, borderTopWidth: 2, borderColor: colors.ink }, step: { minHeight: 130, paddingVertical: 18, flexDirection: "row", alignItems: "flex-start", gap: 13, borderBottomWidth: 1, borderColor: colors.ink }, number: { width: 34, height: 34, backgroundColor: colors.yellow, borderWidth: 1, borderColor: colors.ink, alignItems: "center", justifyContent: "center" }, numberText: { fontWeight: "900" }, body: { ...type.body, color: colors.muted, marginTop: 4 }, action: { padding: 20, gap: 12 } });
